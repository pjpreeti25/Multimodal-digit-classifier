# Multimodal-digit-classifier
Multimodal ML system combining digit images (28×28) with audio features (507-dim) for classification. Uses dual-encoder architecture with fusion. Achieved interpretable results through t-SNE visualization and clustering, showing image embeddings outperform audio. Production-ready modular codebase.
